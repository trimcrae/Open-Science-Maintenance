# GitHub API token used by the scanner (read-only is enough).
# Create at https://github.com/settings/tokens (a fine-grained token with
# public-repo read access, or a classic token with `public_repo`).
GITHUB_TOKEN=
